# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
 + nocsrf
GET     /                           controllers.HomeController.about()

GET     /startGame                  controllers.HomeController.startGame()

GET /fullBoardWurf                  controllers.HomeController.fullBoardWurf()


POST /newMove                       controllers.HomeController.newMove()

POST  /fullBoardGo                  controllers.HomeController.fullBoardGo()


POST /newGame                       controllers.HomeController.newGame()

POST /turn                         controllers.HomeController.turn()

POST /go                            controllers.HomeController.go()


GET /about                          controllers.HomeController.about()

GET /fields                         controllers.HomeController.showFields()

# New -----

GET /fullBoard                     controllers.HomeController.fullBoard()

GET /fieldsJson                      controllers.HomeController.getFields()

POST /fieldsJson                     controllers.HomeController.updateFields()

GET /health                         controllers.HomeController.health()

GET /resetGame                      controllers.HomeController.resetGame()


# POST /fieldsJson                     controllers.HomeController.updateFields()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
